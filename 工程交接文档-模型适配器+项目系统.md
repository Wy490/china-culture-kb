# 工程交接文档 — 全文生成模型适配器 + 项目系统

> 分支: `feat/memory-mosaic-biography-phase5`
> Commit: `ac39d30`（我的收口） + Codex 后续修复（未 commit，在工作区脏状态）
> 日期: 2026-06-10
> 测试: 105 passed, 0 failed

---

## 一、当前工作区状态

工作区有未提交的脏文件——这是 Codex 检查修复后留下的改动，**需要先 commit 再继续工作**。主要包括两类改动：

### A. 我上一轮收口的 Codex 修复（核心逻辑）

| 文件 | Codex 改了什么 |
|------|----------------|
| `story-service.ts` | `isModelSceneBreakdownCompatible` 加强：检查 local/model 双方 scene_ids 唯一性 + 精确集合匹配（不仅是"model IDs subset of local"，改为双向集合相等）；`stripInternalFields` cast 加 `as unknown as` 中间转换 |
| `story-generation-model.ts` | `childEnv` 类型改为 `NodeJS.ProcessEnv`，消除 TS2339/TS2551 |
| `story-regenerate-service.ts` | 删除对 `model_profile_id` 的覆盖（保留原始全文模型 ID）；注释措辞微调 |
| `project-service.ts` | `cleanStory` cast 加 `as unknown as` |
| `validate.ts` | `makeZodLikeError` cast 加 `as unknown as Record` |
| `story-generation-model.test.ts` | 新增 2 个边界测试：重复 scene_id 不兼容、scene_id 缺失+多出不兼容 |
| `tsconfig.json` | 删除 `rootDir` 限制（解决 @shared 和 mcp-server 跨目录类型错误） |

### B. Codex 新增的产品功能（前端 + 路由 + API）

| 文件 | 新增什么 |
|------|----------|
| `web/client/src/views/Home.vue` | 首页改为三卡片工作台布局（知识库/项目/生成），取代旧搜索+省份网格 |
| `web/client/src/views/Knowledge.vue` | 新增知识库浏览页（367 行） |
| `web/client/src/views/Projects.vue` | 新增故事项目列表页（314 行） |
| `web/client/src/views/ProjectDetail.vue` | 新增项目详情页（509 行），含场景重写交互 |
| `web/client/src/api/projects.ts` | 新增 projects API client |
| `web/client/src/api/system.ts` | 新增 `getModelProfiles()` API |
| `web/client/src/views/StoryStudio.vue` | 新增模型选择下拉框 + localStorage 记忆 + 生成结果链接到项目 |
| `web/client/src/views/StoryDetail.vue` | 返回按钮改为"进入故事项目" + "继续生成"双链接 |
| `web/client/src/components/ProvinceGrid.vue` | 省份卡片链接改为 KnowledgeProvince 路由 |
| `web/client/src/App.vue` | 导航新增"知识库"和"故事项目"链接 |
| `web/client/src/router.ts` | 新增 /projects、/projects/:id、/knowledge、/knowledge/:province 路由；旧 /province/:name 重定向到 KnowledgeProvince |
| `web/server/src/routes/projects.ts` | 新增 projects 路由（40 行） |
| `web/server/src/routes/system.ts` | 新增 GET /api/system/models 路由 |
| `web/server/src/index.ts` | 挂载 projects 路由 |
| `web/server/src/__tests__/api.test.ts` | 新增 Projects API 测试 |
| `web/shared/schemas.ts` | 新增 model_profile_id、ProjectIdParamSchema、StorySceneRegenerateRequestSchema |

---

## 二、核心设计决策（不变）

### 全文生成一致性策略

- 模型 scene_breakdown 必须与本地骨架**场景数相等 + scene_ids 双向精确匹配**
- 不兼容 → 整体 fallback 到本地结果，不做部分合并
- 合并时精确按 scene_id 匹配（不再是 find-by-index fallback）

### generation_source 结构化状态

| 字段 | 类型 | 作用 |
|------|------|------|
| `generation_source` | `string` | 可读文案（展示给用户） |
| `generation_mode` | `'external_model' \| 'local_fallback' \| 'local_only'` | 结构化状态（程序判断） |
| `generation_used_fallback` | `boolean` | 前端颜色判断 |

三种状态：`external_model`（绿）、`local_fallback`（橙）、`local_only`（灰）

### 场景重写语义分离

- generation_source/mode/used_fallback **只描述原始全文生成来源**
- 场景重写信息 **只进 reference_trace**
- 不再混成一个语义不清的字段

---

## 三、测试覆盖（105 个）

| 测试文件 | 测试数 | 覆盖范围 |
|----------|--------|----------|
| `api.test.ts` | ~70 | 路由集成、Projects API、Projects 参数验证 |
| `story-generation-model.test.ts` | 16 | 适配器 fallback 7 种 + 兼容性检查 6 种 + 合并逻辑 3 种 |
| `story-regenerate-service.test.ts` | 4 | 场景重写本地/模型/记忆拼图 |
| `model-catalog.test.ts` | ? | 模型目录 |
| `scene-regeneration-bridge.test.ts` | ? | 场景重写桥接 |
| `project-service.test.ts` | ? | 项目服务 |
| `memory-mosaic-service.test.ts` | ? | 记忆拼图 |

---

## 四、下一步建议（新窗口优先处理）

### 必须先做

1. **Commit 脏文件** — 当前 18 个文件有改动未提交，新窗口应先 commit + push
2. **前端项目详情页对接** — `ProjectDetail.vue` 已有 509 行骨架，但需要确认：
   - 场景重写 API 是否已打通（POST /api/projects/:projectId/scenes/:sceneId/regenerate）
   - 生成结果是否正确链接到项目页
   - 项目版本历史展示是否完整

### 建议接下来做

3. **模型选择实际测试** — 设置 `STORY_GEN_COMMAND` 环境变量，用 `claude` CLI 验证真实调用链路
4. **旧故事迁移** — 已存 JSON 缺少 `generation_mode` / `generation_used_fallback`，listStories 和项目列表读取 undefined → 需向后兼容处理
5. **部署文档** — 补充 `STORY_GEN_*` 环境变量说明到 docs

### 不建议做

- 不要继续扩展新功能直到上述 commit + 验证完成
- 不要大改 UI——当前前端三卡片工作台布局已成型

---

## 五、环境变量参考

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `STORY_GEN_PROVIDER` | `command_json` | 适配器 provider |
| `STORY_GEN_COMMAND` | 空（local_only） | 外部命令路径 |
| `STORY_GEN_COMMAND_ARGS` | 空 | 命令参数 JSON |
| `STORY_GEN_COMMAND_TIMEOUT_MS` | `120000` | 适配器超时 |
| `STORY_GEN_AGENT` | `claude` | 桥接用 claude/codex |
| `STORY_GEN_AGENT_MODEL` | 空 | 模型名 |
| `STORY_GEN_AGENT_CLAUDE_PATH` | `claude` | Claude CLI |
| `STORY_GEN_AGENT_CODEX_PATH` | `codex` | Codex CLI |
| `STORY_GEN_AGENT_TIMEOUT_MS` | `180000` | 桥接超时 |

---

## 六、文件清单（按职责）

### 服务端核心

- `web/server/src/services/story-service.ts` — 全文生成主流程 + 兼容性检查 + 合并
- `web/server/src/services/story-generation-model.ts` — 适配器（spawn + Zod + fallback）
- `web/server/src/services/story-generation-prompt.ts` — prompt package 构建
- `web/server/src/services/story-regenerate-service.ts` — 场景重写（不再修改 generation 字段）
- `web/server/src/services/project-service.ts` — 项目 CRUD + 版本管理
- `web/server/src/services/model-catalog.ts` — 模型目录（3 个 profile）
- `web/server/scripts/story-generate-agent-bridge.mjs` — 桥接脚本

### 前端

- `web/client/src/components/StoryResult.vue` — 用布尔字段判断颜色 + 三色（绿/橙/灰）
- `web/client/src/views/Home.vue` — 三卡片工作台
- `web/client/src/views/StoryStudio.vue` — 模型选择 + localStorage 记忆
- `web/client/src/views/ProjectDetail.vue` — 项目详情 + 场景重写交互
- `web/client/src/views/Projects.vue` — 项目列表
- `web/client/src/views/Knowledge.vue` — 知识库浏览
- `web/client/src/api/projects.ts` — 项目 API client
- `web/client/src/api/system.ts` — getModelProfiles

### 共享类型

- `web/shared/types.ts` — GenerationMode + generation_mode/generation_used_fallback 加到 3 个接口
- `web/shared/schemas.ts` — model_profile_id + ProjectIdParam + SceneRegenerateRequest

### 测试

- `web/server/src/__tests__/story-generation-model.test.ts` — 16 个测试

### 路由

- `web/server/src/routes/projects.ts` — 项目 API 路由
- `web/server/src/routes/system.ts` — GET /models 路由

---

## 七、Residual Risk

1. **模型语义一致性** — 适配器成功但模型 full_text 与 scene_breakdown 语义不一致（模型内部问题）
2. **旧故事兼容** — 已存 JSON 无 generation_mode，前端读取 undefined → 显示无模型状态
3. **场景重写 API** — ProjectDetail.vue 的场景重写交互需要确认 API 是否已打通
4. **前端项目页** — 新页面骨架已成型但功能完整性未验证
5. **Codex 脏文件** — 18 个文件改动未 commit，新窗口第一步必须 commit